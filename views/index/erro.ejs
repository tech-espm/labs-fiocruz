<div class="p-4 text-center">
	<h1 class="h2 m-0"><%=((typeof mensagem === "undefined" || !mensagem) ? "A página que você está tentando acessar não foi encontrada" : mensagem) %> 😢</h1>
	<!--
		<%= erro.status %>
		
		<%= erro.stack %>
	-->
</div>
